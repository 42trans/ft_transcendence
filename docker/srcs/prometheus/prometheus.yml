# docker/srcs/prometheus/prometheus.yml
global:
  # デフォルトのスクレイプ間隔
  scrape_interval: 15s 

scrape_configs:
  - job_name: 'nginx'
    scheme: 'http'
    static_configs:
      - targets: ['nginx_exporter:9113']
